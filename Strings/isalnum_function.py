# isalnum function is used to check whether the string contain alpha numeric characters (a-z, 0-9, A-Z)
topic = "WelcomeToPython1"  # it will print false if other characters are present
print(topic.isalnum())
