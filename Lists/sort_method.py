# sort method is used to sort data of list in an order
l = [5, 4, 6, 8]  # only integer data type
l.sort()
print(l)
# reverse in sort is used to reverse the order according to the value
l.sort(reverse=True)
print(l)
