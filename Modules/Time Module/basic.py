import time
# time.time() gives the time since the program is executed
print(time.time())
# time.sleep delays the execution
print(time.sleep(5))
# time.strftime gives the time in a formated way using strings
print(time.strftime("%d/%m/%Y %H:%M:%S"))
